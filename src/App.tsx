import { AppRouter } from './router/AppRouter';

/**
 * Main App component
 * Renders the router which handles all routing and layout
 */
function App() {
  return <AppRouter />;
}

export default App;

